export interface Stroller {
    brand?: string
    color?: string,
    mounth?: string,
    maxWeight?: string,
    description?: string,
    image?: string,
    price?: string,
    key?: string | null
  }